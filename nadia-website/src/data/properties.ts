import type { Property } from '../types';

// All properties are managed in Firestore via the /dashboard.
// This file is intentionally empty  kept so existing imports don't break.
export const properties: Property[] = [];

